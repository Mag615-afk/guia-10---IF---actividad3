name: cusco_360
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.4.0
  cloud_firestore: ^5.4.0
  provider: ^6.0.5
  google_fonts: ^5.0.0
  flutter_localizations:
    sdk: flutter

dev_dependencies:
  change_app_package_name: ^1.1.0
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/placeholders/  # carpeta donde puedes ir pegando imágenes temporales
